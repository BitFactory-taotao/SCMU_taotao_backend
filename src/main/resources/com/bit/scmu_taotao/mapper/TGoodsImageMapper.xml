<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bit.scmu_taotao.mapper.TGoodsImageMapper">

    <resultMap id="BaseResultMap" type="com.bit.scmu_taotao.entity.TGoodsImage">
            <id property="imageId" column="image_id" />
            <result property="goodsId" column="goods_id" />
            <result property="imageUrl" column="image_url" />
            <result property="sort" column="sort" />
            <result property="createTime" column="create_time" />
    </resultMap>

    <sql id="Base_Column_List">
        image_id,goods_id,image_url,sort,create_time
    </sql>

    <select id="selectByGoodsId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from t_goods_image
        where goods_id = #{goodsId}
        order by sort asc
    </select>
</mapper>
