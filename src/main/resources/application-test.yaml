spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/taotao_db?useSSL=false&serverTimezone=UTC
    username: root
    password: ${DB_PASSWORD:taotao_dev_pwd}
  data:
    redis:
      host: localhost
      port: 6379
      password: ${REDIS_PASSWORD:taotao_redis_pwd}

test-only:
  auth:
    enabled: true
    secret: bitscmutaotao
app:
  auth:
    demo-mode: true  # 演示模式开启




